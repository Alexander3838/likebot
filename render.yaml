services:
  - type: web
    name: likebot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    runtime: python
    region: oregon
    plan: free
    pythonVersion: 3.10
